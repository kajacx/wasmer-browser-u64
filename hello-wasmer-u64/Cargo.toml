[package]
name = "hello-wasmer-u64"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
wasmer = { version = "3.1.1", default-features = false, features = ["js-default"] }
